# firmware/src/Makefile — stub; actual build is driven by firmware/Makefile.
# This file exists only so that `make -C firmware/src` does not error.
.PHONY: all clean
all: ; @echo "Use 'make -C firmware' to build"
clean: ; @true
